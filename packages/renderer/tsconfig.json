{
  "extends": "../../tsconfig.base.json",
  "//": "DOM is in `lib` here and nowhere else in packages/: a canvas compositor needs canvas types.",
  "compilerOptions": {
    "lib": [
      "ES2022",
      "DOM",
      "DOM.Iterable"
    ]
  },
  "include": [
    "src"
  ]
}
