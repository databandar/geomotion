{
  "name": "@geomotion/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Primitives shared by everything: ids, coordinates, numeric interpolation. Zero dependencies by contract (ENGINEERING_GUIDE §2).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.7"
  }
}
