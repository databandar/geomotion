# dependencies
node_modules

# build output
dist
.vite
*.tsbuildinfo

# secrets — never commit
.env.local
.env*.local

# generated pipeline artifacts
pipeline/out
pipeline/scripts/_studio-*.json

# AI-generated per-slug illustrations: regenerable inputs, kept out of the repo
# to keep it lean. The convention itself is documented in pipeline/assets/README.md.
pipeline/assets/*/
!pipeline/assets/README.md

# OS / editor
.DS_Store
*.swp
